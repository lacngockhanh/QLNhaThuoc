
@{
    ViewBag.Title = "Danh mục loại chi phí";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
<div ng-app="myApp">
    <div ng-view>

    </div>       
</div>


<script>

    var urlList = '@Url.Action("_pList", "AdminServiceTypes")';
    var urlDetail = '@Url.Action("_pDetail", "AdminServiceTypes")';
    var app = angular.module("myApp", ["ngRoute"]);
    var _domain = '@(Request.Url.Scheme + System.Uri.SchemeDelimiter + Request.Url.Host + (Request.Url.IsDefaultPort ? "" : ":" + Request.Url.Port) + "/")';

</script>

<script src="~/Scripts/AdminServiceTypes/Index.js?v=@Guid.NewGuid()"></script>